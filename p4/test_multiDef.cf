int a;
int a;
void a; //should report multi-decl err + void type err

struct s {
    int a;  //should be fine, as a new scope is applied
    int aa;
    int aa;
    void aa;
};

struct s {  //test duplicated decl of struct
    int bb; //should not be added to struct s
};

void func1() {
    struct s var;
    var.bb = 0;       //should report error, as the later decl is ignored
    var.aa = 0;
}


void func1() {  //test duplicated decl of func
    //with duplicated name, func body should still be analyzed
    int a;      //should be fine
    int fa;
    int fa;     //should report error
}


void func2(int pa, int pa) {    //duplicated params
}
