
struct s1 {
    int num;
};

struct s2 {
    struct s1 next;
};

struct s3 {
    struct s2 next;
};

void function() {

    int a;
    struct bad bad_s;               // bad declaration: struct bad DNE
    struct s3 ss3;

    ss3 = ss3.bad = ss3.next.bad = ss3.next.next.bad;         //invalid field

    a = a.b = ss3.next.next.num.bad;            //dot-access non-struct

    a.a.a ++;             //nested non-struct dot-access

    b.a.a ++;             //nested undecl + non-struct dot-access

}


